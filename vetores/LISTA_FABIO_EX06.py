#6. Leia um número (vetor com 8 elementos) na base binária, calcule e escreva este número na base hexadecimal e na base decimal.

def main():
   vetor = []
   entrada = input('Insira os oito elementos do vetor: ')

   vetor.append(entrada[0:3])
   vetor.append(entrada[3:6])
   vetor.append(entrada[6:8])

   for linha in vetor:
      for numero in linha:
         
   
   print(vetor[2])

main()



